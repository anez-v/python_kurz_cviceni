# udelat fci mult, co vynasobi dve cisla 
def mult (a, b):
    return a * b 
print (mult (10,3))

#miles na km a km na miles
miles = int(input("Míle:"))
def miles_to_km(miles):
    conversion_rate = 0.621371
    return float(miles / conversion_rate)
print(miles_to_km(miles))


# km na miles
km = int(input("Km:"))
def km_to_miles(km):
    conversion_rate = 1.60934
    return float(km * conversion_rate)
print(km_to_miles(km))


#ft na m 
ft = int(input("Stopy:"))
def ft_to_m(ft):
    conversion_rate = 
    return float(ft * conversion_rate)
print(km_to_miles(km))

#m na ft,.... a další 
